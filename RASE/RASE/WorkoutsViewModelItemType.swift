//
//  WorkoutsViewModelItemType.swift
//  RASE
//
//  Created by Sam Beaulieu on 8/19/17.
//  Copyright © 2017 RASE. All rights reserved.
//

import Foundation
/*
// Enum to maintain types of workout view sections
// - Each of these types will correlate to one section in the workout table view
enum WorkoutsViewModelItemType {
    case header
    case equipment
    case progress
    case fundamentals
    case ballHandling
    case defense
    case passing
    case finishing
    case shooting
    case archive
}

func ==(lhs: WorkoutsViewModelItemType, rhs: DrillType) -> Bool {
    switch rhs {
    case .fundamentals:
        if lhs == WorkoutsViewModelItemType.fundamentals { return true }
    case .defense:
        if lhs == WorkoutsViewModelItemType.defense { return true }
    case .passing:
        if lhs == WorkoutsViewModelItemType.passing { return true }
    case .shooting:
        if lhs == WorkoutsViewModelItemType.shooting { return true }
    case .ballHandling:
        if lhs == WorkoutsViewModelItemType.ballHandling { return true }
    case .finishing:
        if lhs == WorkoutsViewModelItemType.finishing { return true }
    }
    return false
}
*/
